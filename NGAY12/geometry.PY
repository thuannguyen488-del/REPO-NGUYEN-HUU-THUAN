# Bài 4 — Module tính diện tích hình học
# Tạo module geometry.py gồm:
# ●	area_circle(r)
# ●	area_rectangle(w, h)
# ●	area_triangle(a, h)   # tam giác có đáy a chiều cao h
def area_circle(r):
    return r**2*3.14
def area_rectangle(w,h):
    return w*h
def area_triangle(a,h):
    return a*h*0.5